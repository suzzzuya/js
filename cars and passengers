/*		A typical car can hold four passengers and one driver,
		allowing five people to travel around. 
		Given n number of people, 
		return how many cars are needed to seat everyone comfortably.
*/
	let passengers = prompt('how many passengers?');
	let cars = passengers / 5
	let result = Math.ceil (cars);
	alert(result);
