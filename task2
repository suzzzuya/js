/*		Create a function that returns true 
		when num1 is equal to num2;
		otherwise return false.
*/	
	num1 = prompt('Enter 1 number');
	num2 = prompt('Enter 2 number');
	if (num1 == num2) {
		alert(true);
	} 
	if (num1 != num2) {
		alert(false);
	} 
	else {
		alert('error');
	}
